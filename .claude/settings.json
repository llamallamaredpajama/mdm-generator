{
  "hooks": {
    "PreCommit": [
      {
        "command": "pnpm -C frontend lint && pnpm -C frontend typecheck && pnpm -C frontend test && pnpm -C frontend build",
        "description": "Run lint, typecheck, tests, and build before commit"
      }
    ]
  }
}
